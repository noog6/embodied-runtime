[project]
name = "embodied-runtime"
version = "0.1.0"
description = "Reusable software runtime for embodied agents"
requires-python = ">=3.13"
dependencies = []

[project.optional-dependencies]
openai = [
    "openai>=3.6,<4",
]

[tool.setuptools.packages.find]
include = ["embodied_runtime*"]
namespaces = false
